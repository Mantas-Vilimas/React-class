import "./CustomerSupport.css";

const CustomerSupportPage = ({ visibility }) => {
  return (
    <div className={`${visibility} customer-support-page`}>
      <p>CustomerSupport</p>
    </div>
  );
};

export default CustomerSupportPage;
