import "./GalleryPage.css";
const GalleryPage = ({ visibility }) => {
  return (
    <div className={`${visibility} gallery-page`}>
      <p>Gallery Page</p>
    </div>
  );
};

export default GalleryPage;
